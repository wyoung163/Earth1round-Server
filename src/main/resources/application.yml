spring:
  profiles:
    default: local
    group:
      local:
        - local
        - common
      dev:
        - dev
        - common
      prod:
        - prod
        - common
---
spring:
  config:
    activate:
      on-profile: common